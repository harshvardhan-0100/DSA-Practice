#include <bits/stdc++.h> 
using namespace std; 

void maxOnes(vector<int> arr) {
    int n = arr.size(); 

    int count = 0; 

    for (int i = 0; i < n; i++) {
        if (arr[i] == 1) {
            count += 1; 
        }
        if (arr[i] == 0) {
            
        }
    }
}

int main() {

}